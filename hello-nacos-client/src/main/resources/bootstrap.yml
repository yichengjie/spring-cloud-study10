server:
  port: 8081
  servlet:
    context-path: /hello-nacos-client
spring:
  application:
    name: nacos-client-app
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
management:
  endpoints:
    web:
      exposure:
        include: '*'
